# Jira API (optional — only needed when using --source jira)
JIRA_BASE_URL=https://your-org.atlassian.net
JIRA_EMAIL=you@example.com
JIRA_API_TOKEN=
JIRA_JQL=project = PROMO AND labels in (Apple, Android, Lightbeam, Roku) ORDER BY priority DESC
